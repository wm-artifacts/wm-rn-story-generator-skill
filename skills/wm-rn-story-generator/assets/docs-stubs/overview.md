# {ComponentName}

<!--
LITERAL TEMPLATE. Copy verbatim into {docsDir}/overview.md and replace the two
placeholders below ({ComponentName}, {componentSlug}). Do NOT include the
AUTO-GENERATED marker — overview.md is the manual stub.

When the update flow detected {existingStoryDir}/docs/overview.md, port its
prose into the sections below before saving.
-->

> One-paragraph summary of what `{ComponentName}` renders and the primary use case it solves in a WaveMaker React Native app. Replace this blockquote with real prose drawn from the runtime source (Phase 2) or the legacy `{existingStoryDir}/docs/overview.md` when porting.

## When to use

- Use case 1
- Use case 2

## Key capabilities

- Capability 1
- Capability 2

## Notes

- Any caveat the reader should know up-front (required service providers, asset dependencies, etc.).
